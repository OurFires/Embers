
import styled from 'styled-components'
import { color,space } from 'styled-system'

const Box = styled.div`
  color:red;

  ${color}
  ${space}
`

// Box.defaultProps = {
//   p:4,
// }


export default Box